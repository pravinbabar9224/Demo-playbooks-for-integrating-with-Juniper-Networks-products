---
- name: Get Device Facts
  hosts: juniper
  roles:
    - Juniper.junos
  connection: local
  gather_facts: yes

  tasks:
    - name: Retrieve facts from device running Junos OS
      juniper_junos_facts:
        host: "{{ inventory_hostname }}"
    - name: Print version
      debug:
        var: junos.version