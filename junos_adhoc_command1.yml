---
- hosts: juniper
  connection: local
  tasks:
    - name: execute Junos command
      junos_command:
        commands:
          - show system uptime
          - show interfaces
          - show configuration
          - show version
      register: output

    - name: print out command
      debug:
        var: output.stdout_lines